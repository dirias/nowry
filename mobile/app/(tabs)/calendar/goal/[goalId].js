/**
 * `/annual-planning/goal/<id>` — one goal and its steps.
 *
 * A pushed screen rather than a segment: the plan is a list of many goals and
 * this is one of them, which is a stack relationship. The web reaches the same
 * thing through a drawer beside its focus-area view.
 */
import { GoalDetail } from '../../../../src/screens/GoalDetail'

export default GoalDetail
