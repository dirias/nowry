/**
 * `/calendar/area/<id>` — one focus area and its goals, pushed over the plan.
 *
 * The web's own path is `/annual-planning/area/:id`; this lives under the Plan
 * tab so a pushed screen keeps the tab it was pushed from (MOB-048).
 */
import { FocusAreaDetail } from '../../../../src/screens/FocusAreaDetail'

export default FocusAreaDetail
