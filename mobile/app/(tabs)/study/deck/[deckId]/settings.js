/**
 * A deck's settings (MOB-078).
 *
 * One step in from the deck, as the web's settings are one item in the deck
 * row's menu. Routes stay thin; the screen lives in `src/screens`.
 */
export { default } from '../../../../../src/screens/DeckSettings'
