/**
 * An active study session (MOB-022).
 *
 * This is the deep-link target: `nowry://study/<deckId>` lands here, which is
 * the same path the web serves at `/study/:deckId`.
 */
export { default } from '../../../src/screens/StudySession'
