/**
 * `/book/<id>` — reading one document.
 *
 * Singular, beside `/books` rather than under it, because that is how the web
 * serves the two: a library and a document are siblings there.
 */
import { BookReader } from '../../../src/screens/BookReader'

export default BookReader
