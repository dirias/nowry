/**
 * `/calendar` — the same route name the web uses, so one link opens the same
 * thing on both (architecture addendum).
 */
import { Calendar } from '../../src/screens/Calendar'

export default Calendar
